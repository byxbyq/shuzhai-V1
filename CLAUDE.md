# 书斋 V66 — 项目上下文

小说创作智能助手，FastAPI 后端 + Vanilla JS SPA 前端。

## Agent skills

### Issue tracker

Issues 以本地 markdown 文件存储于 `.scratch/<feature>/` 目录。详见 `docs/agents/issue-tracker.md`。

### Triage labels

使用默认五级标签：needs-triage / needs-info / ready-for-agent / ready-for-human / wontfix。详见 `docs/agents/triage-labels.md`。

### Domain docs

单上下文布局，CONTEXT.md 位于仓库根目录，ADRs 位于 docs/adr/。详见 `docs/agents/domain.md`。
